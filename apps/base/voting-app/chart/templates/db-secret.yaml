apiVersion: v1
kind: Secret
metadata:
  name: '{{include "voting-app.fullname" .}}-db-secret'
type: Opaque
stringData:
  POSTGRES_USER: '{{.Values.db.secret.user | default "postgres" | quote}}'
  POSTGRES_PASSWORD: '{{.Values.db.secret.password | default "" | quote}}'
