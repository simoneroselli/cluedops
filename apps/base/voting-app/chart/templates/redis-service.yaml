apiVersion: v1
kind: Service
metadata:
  name: '{{include "voting-app.fullname" .}}-redis'
  labels:
    app: '{{include "voting-app.name" .}}-redis'
spec:
  ports:
    - port: {{.Values.service.redis.port | default 6379 | int}}
      targetPort: 6379
      name: redis
  selector:
    app: '{{include "voting-app.name" .}}-redis'
